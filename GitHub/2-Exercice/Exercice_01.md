# Exercice : Git — Rebase, Alias, Commits, .gitignore

## Exercice 1 — Rebase vs Merge
Tu es sur une branche `feature-login` non partagée, en retard de 3 commits sur `main`. Quelle commande utilises-tu pour la mettre à jour proprement, et pourquoi pas un merge ici ?

## Exercice 2 — Alias
Crée un alias Git `unstage` qui retire un fichier de la zone de staging (`git reset HEAD <fichier>`).

## Exercice 3 — Commit conventionnel
Écris un message de commit conventionnel pour : correction d'un bug qui empêchait la connexion OAuth sur le module d'authentification.

## Exercice 4 — .gitignore
Tu as accidentellement commité le dossier `.claude/` avant de l'ajouter au `.gitignore`. Quelles sont les 2 étapes nécessaires pour corriger ça ?
