# Correction : VS Code & Terminal

## Exercice 1
`Ctrl + Shift + L` — sélectionne toutes les occurrences du mot sélectionné dans le fichier.

## Exercice 2
`Shift + Alt + ↓` (ou `↑` pour dupliquer au-dessus).

## Exercice 3
```bash
grep -rl "TODO" --include="*.py" .
```

## Exercice 4
```bash
history | grep git
```
Cela filtre l'historique pour n'afficher que les commandes contenant "git".
